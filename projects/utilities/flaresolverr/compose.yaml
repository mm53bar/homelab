services:
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    network_mode: host
    environment:
      - LOG_LEVEL=info
      - PORT=8191
      - HOST=0.0.0.0
    restart: unless-stopped
